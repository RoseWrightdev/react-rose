import React from "react";
import { render } from "@testing-library/react";

import FlexColtoRow from "./FlexColtoRow";

describe("FlexCol", () => {
  test("renders the FlexColtoRow component", () => {
    render(<FlexColtoRow />);
  });
});