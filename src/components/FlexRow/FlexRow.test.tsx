import React from "react";
import { render } from "@testing-library/react";

import FlexRow from "./FlexRow";

describe("FlexRow", () => {
  test("renders the FlexRow component", () => {
    render(<FlexRow />);
  });
});