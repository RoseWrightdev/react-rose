import React from "react";
import { render } from "@testing-library/react";

import FlexRowtoCol from "./FlexRowtoCol";

describe("FlexCol", () => {
  test("renders the FlexRowtoCol component", () => {
    render(<FlexRowtoCol />);
  });
});