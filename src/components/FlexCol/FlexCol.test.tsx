import React from "react";
import { render } from "@testing-library/react";

import FlexCol from "./FlexCol";

describe("FlexCol", () => {
  test("renders the FlexCol component", () => {
    render(<FlexCol />);
  });
});